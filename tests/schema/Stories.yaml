blocks:
  - name: Stories
    sql: Stories
    measures:
      - name: count
        sql: id
        type: count
    dimensions:
      - name: category
        sql: category
        type: string
      - name: isDraft
        sql: isDraft
        type: boolean
