blocks:
  - name: Country
    table: country
    measures:
      - name: count
        sql: country_id
        type: count
    dimensions:
      - name: name
        sql: country_name
        type: string
